[build-system]
requires = ["setuptools>=77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "vllm-kv-materialization-plugin"
version = "0.1.0"
description = "Dynamic CPU KV materialization decisions for vLLM-HUST"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
